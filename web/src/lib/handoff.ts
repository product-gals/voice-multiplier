// Handoff keys shared between the originator (/write) and the multiplier (/).
// Stored in sessionStorage so they don't persist past the current tab session.
export const PENDING_SOURCE_KEY = "pending_multiplier_source";
